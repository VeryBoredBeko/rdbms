# Это задание пока что не нужно выполнять!

# 🛠️ Проектное Задание — Часть 2  
## Реализация спроектированной базы данных в СУБД (MySQL)

## Уважаемые студенты!

В этой части проекта вам необходимо взять модель, разработанную в Части 1, и реализовать её в реальной СУБД (MySQL).  
Вы должны полностью перенести вашу ER-модель в набор таблиц, связей и ограничений.

---

# 🎯 Цель работы
Создать рабочую базу данных по вашей предметной области и проверить корректность её работы через тестовые запросы.

---

# 📘 1. Создание базы данных

Создайте новую базу данных, имя которой соответствует теме вашего проекта.  
(Код вы пишете самостоятельно.)

---

# 🧱 2. Создание схемы таблиц

На основе ER-диаграммы из Части 1:

- создайте таблицы для всех сущностей;  
- задайте домены (типы данных) для каждого атрибута;  
- определите первичные ключи;  
- определите внешние ключи;  
- установите необходимые ограничения (NOT NULL, UNIQUE, CHECK и др.);  
- создайте промежуточные таблицы для всех связей типа M:N.

Требования к этой части:
- минимум **4 таблицы**;
- таблицы должны полностью совпадать со структурой, созданной вами в Части 1;
- все связи должны быть реализованы корректно.

---

# 🔗 3. Реализация всех связей

Вы должны корректно реализовать:

- связи 1:1 (с уникальными ограничениями при необходимости);  
- связи 1:N;  
- связи M:N через промежуточные таблицы.

Связи должны соответствовать вашей ER-диаграмме.

---

# 🗃️ 4. Заполнение тестовыми данными

Добавьте тестовые данные, чтобы можно было проверить работу модели.

Требования:
- в каждую основную таблицу — **не менее 5–10 записей**;  
- в таблицы связи (M:N) — несколько записей, демонстрирующих реальные связи.

Данные могут быть вымышленными.

---

# 🔍 5. Написание тестовых SQL-запросов

Создайте набор SQL-запросов (минимум 5–8), которые доказывают, что ваша база данных работает.

Ваши запросы должны включать:
- выборку всех данных из таблицы;
- выборку данных с условием;
- сортировку;
- выборку с соединением нескольких таблиц (JOIN);
- запрос, который показывает работу связи M:N;
- использование хотя бы одной агрегатной функции (например, подсчёт количества).

---

# 🧪 6. Проверка корректности системы

Убедитесь, что:
- связи работают корректно (JOIN возвращает правильные данные);
- внешние ключи не позволяют вставлять противоречивые данные;
- ограничения доменов соблюдаются;
- таблицы соответствуют нормальным формам из Части 1.

---

# 📦 Итоговая сдача

В конце вы сдаёте:
1. Короткий отчёт (1–2 страницы), включающий:
   - список созданных таблиц с пояснением их назначения;
   - краткое описание созданных связей;
   - пояснение, как реализованы ограничения доменов.
2. Скриншоты созданных таблиц в MySQL.
3. Скриншоты выполнения тестовых запросов.
4. Тестовые данные (в виде дампа или скриншотов).

---
